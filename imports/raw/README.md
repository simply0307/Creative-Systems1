# Raw imports

Place new, unchanged source files here before classification. Do not rename, optimize, or overwrite originals during intake.

For each intake session, create an ImportBatch JSON record under `src/content/import-batches/`, then create one Artifact JSON record per file under `src/content/artifacts/`.
